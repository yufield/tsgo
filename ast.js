export class Identifier {
    name

    constructor(name) {
        this.name = name;
    }
}

export class ArrayExpression {
    elements

    constructor(elements) {
        this.elements = elements;
    }
}